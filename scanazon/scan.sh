#!/bin/bash
read scan
if [ "$scan" = 4033657720409 ];
then
    echo "Fuck"
else
    echo "Fuck more"
fi

# contains() {
#    [[ $scan =serials.txt (^|[[:space:]])$2($|[[:space:]]) ]] && exit(0) || exit(1)
#}